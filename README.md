This is remote branch which main objective is for web automation for all website can be performed, 

we have BaseTest Class  - Driver intilization and Driver closing(DriverFactory class is used here)

each web page iE: Amazon - we have to have Amazon test which extends the base test class and initialize the amazone page class prior @test - @BeforTest

Common class contains basic element handling methods in adition we have to add to the same file,

Functional methods we have to add extra files to the utils package if needed.

Overall we using : Java selenium TestNG Maven 
